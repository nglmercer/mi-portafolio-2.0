<nav transition:animate="none" class="no-print">
  <a href="/mi-portafolio">mi portafolio</a>
  <a href="/mi-portafolio/dos">Dos</a>
  <a href="/mi-portafolio/tres">Tres</a>
  <a href="/mi-portafolio/cuatro">Cuatro</a>
  <a href="/mi-portafolio/carouselhtml">Proyectos</a>
  <label for="themeToggle" class="toggle no-print">
    <input type="checkbox" id="themeToggle" class="no-print toggle">
    <svg width="30px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
      <path class="sun no-print" fill-rule="evenodd" d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"/>
      <path class="moon no-print" fill-rule="evenodd" d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"/>
    </svg>
  </label>
</nav>


<style is:inline>
    .toggle:hover {
      background-color: #23232344;
    }
    .dark .sun {
    fill: #f6ff00; /* Cambia el fill color deseado para el elemento con clase "moon" en modo oscuro */
    }
    .dark .moon {
    fill: #ffffff00; /* Cambia el fill color deseado para el elemento con clase "moon" en modo oscuro */
    }
    .sun {
      fill: rgba(246, 255, 0, 0); /* Color predeterminado para .sun en modo claro */
    }

    html.dark {
      color: #ffffffdb;
    }
    :global(.dark) .sun { fill: transparent; color:transparent}
    :global(.dark) .moon { fill: white; }

    /* Agrega más selectores CSS para otros elementos que deseas cambiar de color en modo oscuro */
  </style>
  
  <!-- <script is:inline>
    var toggletheme = document.getElementById('themeToggle');
    if (toggletheme) {handleToggleClick()}

    function handleToggleClick() {
      if (toggletheme.checked) {
      document.documentElement.classList.add("dark");
      } else {
        document.documentElement.classList.remove('dark');
      }
      console.log(toggletheme.checked)
      var isDark = toggletheme.checked;
      document.documentElement.classList.toggle("dark", isDark);
  
      // Apply the ".dark" class to all elements (consider a more targeted approach if needed)
      var allElements = document.getElementsByTagName("*");
      for (let i = 0; i < allElements.length; i++) {
        var el = allElements[i];
        el.classList.toggle("dark", isDark);
      }
    }
  
    // Attach event listener to the checkbox
    toggletheme.addEventListener("change", handleToggleClick);

    // Attach event listener to the checkbox
    document.getElementById("themeToggle").addEventListener("change", handleToggleClick);
    function guardarEstadoCheckboxes() {
        var checkboxes = document.querySelectorAll('input[type="checkbox"]');
        checkboxes.forEach(checkbox => {
            localStorage.setItem(checkbox.id, checkbox.checked);
        });
    }

    // Función para aplicar el estado guardado de los checkboxes al cargar la página
    function aplicarEstadoCheckboxes() {
        var checkboxes = document.querySelectorAll('input[type="checkbox"]');
        checkboxes.forEach(checkbox => {
            var estadoGuardado = localStorage.getItem(checkbox.id);
            if (estadoGuardado !== null) {
                checkbox.checked = 'true';
            }
            handleToggleClick()
            //console.log(estadoGuardado);
        });
    }
    window.addEventListener('load', aplicarEstadoCheckboxes);
        
    // Llamar a la función para guardar el estado de los checkboxes antes de salir de la página
    window.addEventListener('beforeunload', guardarEstadoCheckboxes);
  </script>
   -->
<style>
nav {
  display: flex;
  justify-content: center;
  align-items: center;
  position: fixed;
  width: 100vw;
  height: 50px;  /* Set a specific height for the navbar */
  gap: 10px;
  background-color: #cde6fa86;  /* Add a background color */
  box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);  /* Add a subtle shadow */
  font-size: 16px;  /* Increase font size for better readability */
  z-index: 100;
}

/* Style the anchor links within the navbar */
nav a {
  color: #333;  /* Set a default text color */
  text-decoration: none;  /* Remove underline */
  padding: 10px 15px;  /* Add some padding for better click area */
  border-radius: 5px;  /* Add rounded corners */
  transition: all 0.2s ease-in-out;  /* Add smooth hover effect */
}

/* Style the anchor links on hover */
nav a:hover {
  background-color: #ddd;  /* Change background color on hover */
  color: #000;  /* Change text color on hover */
}
</style>
